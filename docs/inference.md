# Inference

The generator loads a self-contained model bundle and supports:
- greedy decoding
- temperature
- top-k
- top-p
- repetition penalty
- stop sequences

The current generation path prioritizes correctness and clarity. KV cache is a deliberate future extension.
